{
  "name": "react-webpack-decompiler",
  "version": "1.0.0",
  "tasks": {
    "soft-cache-clear": "npx rimraf ./app && npx rimraf ./.cache/Transformer-* && npx rimraf ./.cache/WebpackChunk && npx rimraf ./.cache/WebpackModule",
    "main": "deno task soft-cache-clear && deno task main-cached",
    "main-cached": "npx rimraf ./output.log && deno run --allow-net --allow-write --allow-sys --allow-read --allow-env src/main.ts",
    "test": "deno run --allow-net --allow-write --allow-sys --allow-read --allow-env src/test/test.ts"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
